---
description: 强制禁止源文件中多余空行（全局，写入/保存前必检）
alwaysApply: true
---

# 空行格式（强制）

**绝不允许**在源文件中出现「每行代码/注释之间都插一行空白」或连续多个空行。本规则对 Agent **写入、覆盖、批量替换** 一律生效；保存前必须自检。

---

## 一、禁止模式（❌）

### 1.「隔行空行」——最常见错误

每一行有效内容后紧跟一整行空白，导致文件行数翻倍：

```csharp
// ❌ 禁止
using SqlSugar;

using Takt.Shared.Enums;

public class TaktCompany
{
    public string CompanyCode { get; set; }
}
```

```typescript
// ❌ 禁止
export function getUserList() {

  return request.get('/api/users');

}
```

### 2. 连续多个空行

```text
❌ 禁止超过 1 个连续空行（除文件末尾可不保留尾换行外的刻意分段）
```

### 3. 无意义首尾空行

- 文件开头：文件头注释之前不得有空行（首字符应为 `/` 或 `using` 等）
- 块内部：属性/方法之间**最多** 1 个空行分隔

---

## 二、允许的空行（✅）

| 场景 | 规则 |
|------|------|
| `using` 与 `namespace` 之间 | **至多 1 行** |
| 文件头 `// =====` 块与 `using` 之间 | **至多 1 行** |
| 相邻类型/成员之间 | **至多 1 行** |
| 逻辑段落（如 `#region`、种子字典项之间） | **至多 1 行** |
| Vue `<script>` / `<template>` 分区注释前 | **至多 1 行** |

**不允许**用空行分隔「同一属性」的 XML 注释三行、同一 `SugarColumn` 与 `public` 属性之间等——它们应紧挨，中间无空行。

---

## 三、参照密度（✅）

与仓库内正常文件对齐，例如：

- `@backend/src/Takt.Domain/Entities/Identity/TaktUser.cs`
- `@backend/src/Takt.Infrastructure/Data/Seeds/EntitySeedData/TaktCultureSeedData.cs`
- `@frontend/src/api/identity/user-api.ts`

同类文件行数应接近（同逻辑体量下）；若新文件行数约为「有效行数 × 2」，视为违反本规则，**必须重写为紧凑格式**。

---

## 四、Agent 强制流程

1. **写入前**：按本节排版，禁止工具输出「隔行空行」格式。
2. **写入后**：对修改过的文件目视或统计——任意相邻两行若均为空白，或有效行之间出现多余空行，**立即修正后再结束任务**。
3. **批量替换 / 生成脚本输出**：落盘前合并连续空行为单行、删除属性/注释之间的空行。
4. **仅改格式时**：不得顺带改动业务逻辑；只删多余 `\n`、整理空行。

---

## 五、自检清单（提交前）

- [ ] 无「一行代码 + 一行空白」交替模式
- [ ] 无 2 行及以上连续空白行
- [ ] 文件头、`using`、`namespace`、类成员空行符合 §二
- [ ] 与同目录参照文件空行风格一致

**违反本规则视为未完成修改，不得交付。**
