---
description: Takt 表单组件 xxx-form.vue 规范（单表/主子表/树表）；由 index.vue 弹窗引用；叠加 12-crud
globs: frontend/src/views/**/components/*-form.vue
alwaysApply: true
---

# Vue 表单组件（*-form.vue）

通用 UI/i18n 见 `@.cursor/rules/02-frontend.mdc`；列表页壳见 `13-vue-view`；DTO 对齐见 `12-crud`。

**前置（强制）**：先满足 `12-crud`；主子表/树表叠加 `10-master-detail` / `11-tree-table`。

| 表形态 | 表单特征 | 参照 |
|--------|----------|------|
| **单表** | 单 `a-form` 或 `a-tabs` 分业务段 | `user-form.vue` |
| **主子表** | Tab 主表 + Tab/表格内嵌子表行 | `dict-type-form.vue` |
| **树表** | `TaktTreeSelect` 选 `parentId` | `dept-form.vue` |

---

## 一、通用约定（三种形态均须）

### 1.1 文件

- 路径：`views/{模块}/components/{entity}-form.vue`
- 顶部 **8 行 HTML 注释**（功能描述含 `defineExpose` 清单）
- 文件名 **kebab-case**；模板标签 `<user-form />`

### 1.2 Props

```typescript
/** 父级传入的编辑 DTO；新增时为 undefined 或空对象 */
formData?: TaktXxxDto | null
/** 父级提交 loading，禁用表单项 */
loading?: boolean
```

- 主键判断：`formData?.id` / `formData?.xxxId`（**string**）
- 编辑态只读字段：`:disabled="!!formData?.xxxId"`（业务码等）

### 1.3 defineExpose（强制）

父级 `index.vue` 通过 `ref="formRef"` 调用：

| 方法 | 说明 |
|------|------|
| `validate` | `async () => void`；校验失败 throw |
| `getValues` | `() => TaktXxxCreateDto \| TaktXxxUpdateDto`；映射为 API 入参 |
| `resetFields` | 清空/恢复初始态 |
| `setServerValidationErrors` | （可选）FluentValidation 字段错误回填 |

参照 `user-form.vue`、`dept-form.vue`。

### 1.4 表单 UI

- 根：`a-form` + `layout="horizontal"` + `label-align="right"`
- 栅格：`a-row` / `a-col`（`:gutter="24"`）
- 文案：`t('entity.*')` 标签；`common.page.form.placeholder.*` 占位
- 字典：`TaktSelect` + `api-url` 或 `dict-type`（与后端 `DictTypeCode` 一致）
- **「xxx内容」正文**：`takt-rich-editor`（字段名 `content` / `XxxContent`，或 XML 摘要标题以「内容」结尾；备注 `remark` 仍用 `a-textarea`）
- **文件名称 + 访问地址**：同表同时有 `fileName` 与 `accessUrl` 时用 `takt-upload-file`（上传后回填 `fileName`，禁止手输）；**排除** Foundation `TaktFile` 自身。查询抽屉仍用普通输入；子表行内单元格仍用输入框
- **TaktNumbering 自动编码（表单）**：实体字段 XML 描述含「自动通过 TaktNumbering」且**不含** MIME → 新增态 **编码规则下拉**（`TaktSelect` + `TaktNumberings/options`，`:api-params="{ documentType: '<菜单 MenuName>' }"` 仅显示该单据类型规则，字段 `numberingRuleCode`）+ **业务编码只读预览**（`previewNumberingNext` / `useTaktFormNumbering`）；编辑态规则隐藏、编码 `disabled`。`DocumentType` 存 **MenuName**（非 Id）。文案：`common.page.form.numberingRule` / `numberingCodePreview` / `numberingRuleRequired`。参照 `announcement-form.vue` / `employee-form.vue`。❌ 禁止按类型/性别猜规则码；❌ 禁止手输业务编码；❌ 禁止无 `documentType` 过滤拉全量规则
- **TaktNumbering 自动编码（上传引擎）**：字段描述含「自动通过 TaktNumbering」且含 MIME → **不走表单选规则**；由 `TaktFileUploadEngine` 按 MIME→`FD-F*` 调 `ITaktNumberingGenerator`
- 长表单：`a-tabs` 分 Tab；Tab 文案用 `{路径}.page.tabs.*` 静态 locales

❌ 在 form 内再包 `TaktModal`；❌ 硬编码中文；❌ 表单控件 `size="small"`（须 Ant Design 默认 `middle`）；❌ 隔离/只读字段用 HTML `readonly`（须 `disabled`，参照 `user-form.vue`）；❌ 大块 scoped 布局 CSS（Tailwind / 极简 `:deep` 例外见 `02-frontend` §1.2）

---

## 二、单表表单

**参照**：`views/identity/user/components/user-form.vue`

### 2.1 结构

```text
a-form（或 a-form 外包 a-tabs）
  → a-form-item + a-input / TaktSelect / a-input-number / takt-rich-editor / takt-upload-file …
  → 业务复杂时分 a-tab-pane
```

### 2.2 状态

- `formState`：`reactive` 或 `ref`，字段与 Create/Update DTO 对齐
- `rules`：`computed` 或常量；必填与后端 Validator 一致
- `watch(formData)`：编辑时灌入；新增时 `resetFields`

### 2.3 getValues

- 仅返回表单字段；密码/hash、外键等由父级或本组件内映射
- 不把 `id` 写入 Create DTO；Update 时父级传路径 `{id}`

---

## 三、主子表表单

**参照**：`views/foundation/dict/components/dict-type-form.vue`

### 3.1 结构

```text
a-tabs
  Tab「主表」→ a-form（mainFormRef / mainFormState / mainFormRules）
  Tab「子表」→ a-table 行内编辑 或 子 form 列表
```

### 3.2 子表行

- 内存数组：`dictDataList` / `items`；与 `TaktXxxCreateDto.items` 对齐
- 临时行 `row-key`：`client-${uuid}`；已持久化用实体 Id **string**
- 行内增删改：工具栏「添加行」+ 行内 `a-input` / 操作列删除
- 复杂子表：拆 `xxx-item-form.vue` 或独立 `DictDataWindow`（见 `13-vue-view` §3.1）

### 3.3 validate / getValues

```typescript
// validate：先主表，再校验子表行（非空、唯一键等）
await mainFormRef.value?.validate()
// getValues：{ ...mainFormState, dictDataList / items: [...] }
```

- 一次提交：`createXxx` / `updateXxx` 携带子表数组（与后端 CreateDto 一致）
- 禁止仅保存主表而静默丢弃子表变更

---

## 四、树表表单

**参照**：`views/human-resource/organization/dept/components/dept-form.vue`

### 4.1 必备字段

| 字段 | 组件 |
|------|------|
| `parentId` | `TaktTreeSelect`，`api-url="/api/TaktXxxs/tree-options"` |
| `sortOrder` | `a-input-number`，`:min="0"` |
| 业务编码 | 新增可编辑；编辑 `:disabled="!!formData?.xxxId"` |

### 4.2 父级默认值

- `index.vue` 打开新增弹窗时：若左侧树有选中节点，传入 `formData.parentId = selectedTreeKey`（string）
- `getValues` 返回的 `parentId`：**string**；根节点用 `"0"` 或与后端约定一致

### 4.3 防环（前端辅助）

- 编辑时 `TaktTreeSelect` 排除自身及子孙（若组件支持 `treeCheckStrictly` / 自定义 filter）
- 最终环检测以后端为准；前端校验失败须 `t('validation.*')` 或静态 `*.page.*` 提示

---

## 五、父级 index.vue 集成

```vue
<TaktModal v-model:open="formVisible" :confirm-loading="formLoading" @ok="handleFormSubmit">
  <XxxForm ref="formRef" :form-data="formData" :loading="formLoading" />
</TaktModal>
```

```typescript
async function handleFormSubmit() {
  await formRef.value?.validate()
  const dto = formRef.value?.getValues()
  formLoading.value = true
  try {
    if (formData.value?.id) await updateXxx(formData.value.id, dto)
    else await createXxx(dto)
    formVisible.value = false
    await loadData()
  } finally {
    formLoading.value = false
  }
}
```

---

## 六、三种形态对照

| 项 | 单表 | 主子表 | 树表 |
|----|------|--------|------|
| 根结构 | 单 form / tabs | tabs：主 form + 子 table | tabs 或单 form |
| 外键 | TaktSelect | 子表 xxxId | TaktTreeSelect parentId |
| validate | 单 formRef | mainFormRef + 子表规则 | 单 formRef |
| getValues | 扁平 DTO | 主表 + items[] | 含 parentId string |

---

## 七、代码生成（generate-vue-*-from-api.cjs）

详见 `15-codegen` §3.6。与 `index.vue` 同步生成 `components/{entity}-form.vue`（有 Create/Update API 时）。

| 形态 | 脚本产出 |
|------|----------|
| **单表** | CreateDto 字段 → `a-form`；超 10 字段自动 Tabs；隔离字段只读 |
| **主子表** | 主表 Tab + 子表 Tab（`a-table` 行内编辑、`handleAdd/Remove*Row`） |
| **树表** | **不生成**；手工 `TaktTreeSelect` + `parentId`（§四） |

`defineExpose` 脚本已含 `validate` / `getValues` / `resetFields`；复杂页（如 `user-form`）在排除列表内须手工维护。

---

## 八、Agent 约束

- 新建表单：先定形态（单表/主子/树），再读本章对应 § + 参照文件
- 脚本生成后：**必须**核对 getValues 与 Create/UpdateDto、子表数组字段名
- **必须** `defineExpose({ validate, getValues, resetFields })`；复杂页加 `setServerValidationErrors`
- 主子表：**必须** getValues 含子表数组；与后端 Create/UpdateDto 字段名一致
- 树表：**必须** parentId 为 string + tree-options API
- 字段标签优先 `entity.*`；Tab/页面专有文案 `{路径}.page.*`
- 「xxx内容」字段：**必须** `takt-rich-editor`，禁止用 `a-input` / `a-textarea` 代替
- 同时有 `fileName` + `accessUrl` 的业务实体：**必须** `takt-upload-file`，`fileName` 仅上传回填；禁止给 `TaktFile` 自身生成该控件
- 实体字段含「自动通过 TaktNumbering」且无 MIME：**必须** 规则下拉 + 只读编码（`useTaktFormNumbering`）；含 MIME 的文件编码走上传引擎，不生成表单规则选择
- 实现清单：`.cursor/skills/14-vue-form/SKILL.md`
