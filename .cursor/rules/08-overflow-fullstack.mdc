---
description: 前后端联动溢出陷阱与协作规范（Vue + C#）；与 00-project 一并始终生效
alwaysApply: true
---

# 前后端联动溢出陷阱

**真正的安全来自前后端共同设限，而不是某一端「扛住」。**

细则分层：

| 层次 | 规则文件 |
|------|----------|
| Vue 渲染/数据/数字 | `@.cursor/rules/07-overflow-vue.mdc` |
| C# 分页/内存/算术 | `@.cursor/rules/06-overflow-csharp.mdc` |
| 联合架构（本节） | 本文件 |

通用五条铁律见 `@.cursor/rules/00-project.mdc` §五。

---

## 一、经典事故链（禁止）

```text
Vue 一次请求全量数据
  → C# 查全表 + EF 跟踪实体
  → JSON 序列化巨大 payload
  → 浏览器内存暴涨
  → 页面卡死 / 崩溃
```

**任一环不设限，整条链都会炸。**

---

## 二、正确架构

```text
Vue（分页 + 必要时虚拟列表）
  ↓
API（分页 + 过滤 + DTO 裁剪）
  ↓
C#（IQueryable + Select 投影 + Take）
  ↓
DB（索引 + 条件在库内执行）
```

---

## 三、前后端协作规范（强制）

### Vue（前端）

| 规则 | 说明 |
|------|------|
| ✅ | 所有列表 **必须分页** |
| ✅ | **不信任**后端返回的数据长度与字段体积 |
| ✅ | **不做**前端全量排序替代服务端排序 |
| ✅ | 主键/长整型用 **`string`**，与后端 DTO 一致 |

### C#（后端）

| 规则 | 说明 |
|------|------|
| ✅ | 所有集合接口 **必须有上限** |
| ✅ | 所有分页参数 **必须 clamp 校验** |
| ✅ | 返回 **DTO 裁剪字段**，禁止实体整表输出 |
| ✅ | 导出/报表：**流式或分批**（`TaktExcelHelper`），禁止一次性全量 |

---

## 四、统一处理策略

| 场景 | Vue | C# |
|------|-----|-----|
| 数据太多 | 虚拟列表 | 分页 + `Take` |
| 数字太大 | `string` / `BigInt` | DTO 用 `string` 传 `long` |
| 内存暴涨 | 不拉全量 | 不 `ToList()` 全表 |
| 栈溢出 | 递归改循环、限树深 | 限制递归深度 |
| JSON 爆炸 | 不解析巨型 payload | DTO 投影、分页 |

---

## 五、审查清单（联调 / Code Review）

1. 该列表是否 **前后端都分页**？是否存在「前端一页、后端全表」？
2. 主键/雪花 ID 是否 **string 贯穿** types ↔ DTO ↔ JSON？
3. 导出/导入是否有 **行数/文件大小** 双端上限？
4. 是否假设「数据量不大」而省略虚拟列表或 DB 分页？
5. 溢出/越界/OOM 是否按 **bug** 修，而非 catch 后静默成功？

---

## 六、Agent 约束

- 改列表/导出/报表：同时检查 `07-overflow-vue` 与 `06-overflow-csharp`
- 禁止只改一端设限而另一端仍全量
- 新增 API 默认分页；全量接口须在注释中说明理由与硬上限
- 本域实现清单：`.cursor/skills/08-overflow-fullstack/SKILL.md`
