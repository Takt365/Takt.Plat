---
description: Takt CRUD 全栈基线（单表/主子表/树表均遵守）；命名/路由/权限/标准能力/页面壳；与 00~11 一并生效
globs: backend/**/*.cs,frontend/src/api/**/*,frontend/src/types/**/*,frontend/src/views/**/*
alwaysApply: true
---

# CRUD 全栈基线

通用命名、权限、i18n 见 `@.cursor/rules/00-project.mdc`。后端分层细则见 `01-backend`；前端 UI 细则见 `02-frontend`；分页/溢出见 `06`~`08`。

**本规则为所有列表类模块的强制基线**——单表、主子表、树表均须先满足本章，再叠加各自扩展规则。

| 表形态 | 代码生成模板 | 基线（本章） | 扩展规则 |
|--------|--------------|--------------|----------|
| **单表** | `crud` | ✅ 仅本章 | — |
| **主子表** | `sub` | ✅ 主表 + 子表实体各一套 CRUD | `10-master-detail`（级联 Fill/Save、展开/抽屉 UI） |
| **树表** | `tree` | ✅ 节点实体完整 CRUD | `11-tree-table`（ParentId、`GetXxxTreeAsync`、左树右表 UI） |

审批流业务单据另见 `09-workflow`（在 CRUD 之上集成流程）。

---

## 一、全栈参照（先读再写）

| 层 | 参照 |
|----|------|
| 后端服务 | `TaktLoginLogService.cs`（标准 CRUD + 导入导出） |
| 后端控制器 | `TaktLoginLogsController.cs`、`TaktUsersController.cs` |
| 实体 | `TaktUser.cs` |
| 前端 API | `api/identity/user.ts` |
| 前端页面 | `views/identity/user/index.vue` + `components/user-form.vue` |

---

## 二、命名与路由对齐（强制）

| 层 | 规则 | 用户示例 |
|----|------|----------|
| 应用服务 | `Takt` + **单数** + `Service` | `TaktUserService` |
| 控制器 | `Takt` + **复数** + `Controller` | `TaktUsersController` |
| API 路由 | `api/[controller]` | `api/TaktUsers` |
| 前端 `*_API_BASE` | 与控制器类名一致（复数） | `TaktUsers` |
| 前端 api 文件 | kebab 单数 | `user.ts`（非 `users.ts` 时以生成脚本为准） |

**方法名必须带实体前缀**（禁止 `GetListAsync` / `getList`）。

---

## 三、标准能力映射表

| 功能 | C# 服务 | HTTP | 控制器路由 | 前端 API | 权限末段* |
|------|---------|------|------------|----------|-----------|
| 分页列表 | `GetXxxListAsync` | GET | `list` | `getXxxList` | `list` |
| 详情 | `GetXxxByIdAsync` | GET | `{id}` | `getXxxById` | `query` |
| 下拉选项 | `GetXxxOptionsAsync` | GET | `options` | `getXxxOptions` | `query` |
| 新增 | `CreateXxxAsync` | POST | — | `createXxx` | `create` |
| 更新 | `UpdateXxxAsync` | PUT | `{id}` | `updateXxx` | `update` |
| 单删 | `DeleteXxxByIdAsync` | DELETE | `{id}` | `deleteXxxById` | `delete` |
| 批删 | `DeleteXxxBatchAsync` | DELETE | `batch` | `deleteXxxBatch` | `delete` |
| 状态 | `UpdateXxxStatusAsync` | PUT | `{id}/status` | `updateXxxStatus` | `update` |
| 导入模板 | `GetXxxTemplateAsync` | GET | `template` | `getXxxTemplate` | `import` |
| 导入 | `ImportXxxAsync` | POST | `import` | `importXxxData` | `import` |
| 导出 | `ExportXxxAsync` | GET | `export` | `exportXxxData` | `export` |

\* 完整权限码：`领域:目录[:子目录…]:实体:操作`（`00-project` §1.7）。菜单种子、`[TaktPermission]`、`v-permission`、工具栏 `*-permission` **四处一致**。

---

## 四、后端清单

**单表**：一套 Entity + Service + Controller。  
**主子表**：主表、子表各一套（如 `TaktDictTypes` + `TaktDictDatas`），子表同样具备本章能力映射。  
**树表**：节点实体仍须 `GetXxxListAsync` / `CreateXxxAsync` 等；另增 `GetXxxTreeAsync`（见 `11-tree-table`）。

```
Domain     TaktXxx.cs（Tenant/Company/Approval 基类；表名 takt_{模块}_{实体}）
DTO        TaktXxxDto / QueryDto:TaktPagedQuery / Create / Update / Status / Template / Import / Export
Service    ITaktXxxService + TaktXxxService : TaktServiceBase
方法       上表全部能力（不支持须在类注释说明）
列表       QueryExpression + GetPagedAsync + Adapt + TaktPagedResult.Create
详情       租户/公司隔离校验；不存在 return null 或 ThrowBusinessExceptionLocalized
Validator  FluentValidation（Application）
Controller TaktXxxsController : TaktControllerBase；只注入 ITaktXxxService
异常       try/catch + HandleException；成功 Success(...)
种子       菜单 Permission + entity.* i18n（按需）
```

❌ 控制器内 SqlSugar；❌ 全表 ToList 返前端（见 `06-overflow-csharp`）。

---

## 五、前端清单

**视图/表单细则**见 `@.cursor/rules/13-vue-view.mdc`、`14-vue-form.mdc`；本节为 CRUD 基线摘要。

**单表**：`TaktSingleTable` + 标准页面壳（`13-vue-view` §二）。  
**主子表**：主表页壳 + `13-vue-view` §三 + `10-master-detail`。  
**树表**：`13-vue-view` §四 + `11-tree-table` + `14-vue-form` §四。

```
api/       getXxxList…；import request from '@/api/request'；路径复数 TaktXxxs
types/     id 及 long 字段 string；TaktPagedQuery / TaktPagedResult
views/     index.vue（8 行 HTML 头）+ components/xxx-form.vue
页面分区   查询栏 → 工具栏 → 表格 → 分页 → 弹窗 → 高级查询/列设置
组件       TaktQueryBar、TaktToolsBar、TaktSingleTable、TaktPagination、takt-modal
表格       row-key string；:pagination="false" + 外置 TaktPagination
权限       TaktToolsBar *-permission + 按钮 v-permission
文案       列标题 entity.*；页面壳 locales/{域}/{模块}/page.* 或 common.page.*
导入导出   template/import blob；export responseType blob
loading    列表/提交 async + loading 态
```

❌ 硬编码中文；❌ 双层 a-modal；❌ pageSize 9999 全量拉数。

---

## 六、标准页面交互

| 操作 | 行为 |
|------|------|
| 查询/重置 | 重置 pageIndex=1 → `getXxxList` |
| 新增 | 空表单 → `takt-modal` → `createXxx` → 刷新列表 |
| 编辑 | 单选行 → `getXxxById` → 表单 → `updateXxx` |
| 删除 | 确认框 → `deleteXxxById` / `deleteXxxBatch` |
| 导入 | 下载 `getXxxTemplate` → 选文件 FormData → `importXxxData` |
| 导出 | 当前查询条件 → `exportXxxData` |
| 状态开关 | 行内 switch → `updateXxxStatus`（若实体有状态字段） |

表单组件 `defineExpose`：`validate`、`getValues`、`resetFields`（详见 `14-vue-form` §1.3；参照 `user-form.vue`）。

---

## 七、代码生成（可选）

**权威文档**：`@.cursor/rules/15-codegen.mdc`；一键入口 `generate-all.cjs`。

| 脚本 | 产出 |
|------|------|
| `generate-all.cjs` | 全栈 8 步流水线 |
| `generate-from-backend.cjs` | 前端 `api/`、`types/` |
| `generate-vue-all-from-api.cjs` | `index.vue` + `*-form.vue`（串联 CRUD / TREE / Master-Detail） |
| `generate-services-from-dtos.cjs` | 服务 |
| `generate-controllers-from-services.cjs` | 控制器 |

| 形态 | 脚本识别 | Vue 是否自动生成 |
|------|----------|------------------|
| 单表 | 默认 | ✅ |
| 主子表 | 实体 `OneToMany` | ✅（展开行 + 表单 Tab） |
| 树表 | 实体 `ParentId` | ❌ 手工 `13-vue-view` / `14-vue-form` |

生成后须人工对照本章 + `13`/`14`/`10`/`11` 补：权限、QueryExpression、树表/排除实体 UI。

---

## 八、交付自检（全栈）

1. 上表 **11 项能力** 前后端命名与路由是否一一对应？
2. 权限码在菜单、控制器、`v-permission`、工具栏是否一致？
3. 主键 `long` ↔ 前端 `string` 是否贯穿 types/DTO/JSON？
4. 列表是否 **双端分页**（`08-overflow-fullstack`）？
5. 空行格式（`03-format-blank-lines`）？

---

## 九、Agent 约束

- **任何**新建/审查列表模块：先对照本章能力映射与交付自检
- 单表：本章 + `13-vue-view` + `14-vue-form`
- 主子表：本章（主+子双实体 CRUD）+ `10-master-detail` + `13-vue-view` / `14-vue-form`
- 树表：本章（节点 CRUD）+ `11-tree-table` + `13-vue-view` / `14-vue-form`
- 代码生成：`15-codegen`（`generate-all`）；树表/排除实体 Vue 须手工
- 实现清单：`.cursor/skills/12-crud/SKILL.md`
