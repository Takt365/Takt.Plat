---
description: Takt 树表（ParentId 层级）前后端规范；与 00-project、01-backend、02-frontend、07-overflow 一并生效
globs: backend/**/TaktDept*.cs,backend/**/TaktMenu*.cs,backend/**/*Tree*.cs,frontend/src/components/business/takt-tree-*/**/*,frontend/src/views/human-resource/organization/dept/**/*
alwaysApply: true
---

# 树表（Tree Table）规范

**强制基线**：树节点实体须遵守 `@.cursor/rules/12-crud.mdc`。本章规定 **ParentId 层级与树形 UI 扩展**。

**代码生成**：实体含 `ParentId` 时，`generate-all` 生成 `*TreeDto`、`Get*TreeAsync`、`tree`/`tree-options` 路由（`15-codegen` §二）；**Vue 不自动生成**（`Dept` 等在排除列表），须手工 `13-vue-view` §四 + `14-vue-form` §四。

通用命名、权限、i18n 见 `@.cursor/rules/00-project.mdc`；后端见 `01-backend`；前端见 `02-frontend`；树深/虚拟列表见 `07-overflow-vue` §3。

**代码生成模板类型**：`TaktGenTable.GenTemplateCategory = tree`（对应 `dict.gen.template.type.tree`）。

---

## 一、适用场景

| 场景 | 示例 |
|------|------|
| 组织/分类层级 | 部门、菜单、成本中心、科目 |
| 左树右表 | 左侧导航树 + 右侧树形表格（部门管理） |
| 树选择 | `TaktTreeSelectOption` 下拉/表单选父节点 |

❌ 单据头+明细（外键一对多）→ 见 `@.cursor/rules/10-master-detail.mdc`

---

## 一·五、与 CRUD 基线的关系

树模块 **不是** 只有 `tree` 接口——节点实体仍须完整 CRUD，参照 `TaktDeptService`：

| 能力 | 树模块中的落点 |
|------|----------------|
| `GetXxxListAsync` | 平铺分页列表（可选，与树并存） |
| `GetXxxByIdAsync` / `CreateXxxAsync` / `UpdateXxxAsync` / `DeleteXxxByIdAsync` | 表单增删改、拖拽改 `ParentId` |
| `GetXxxTreeAsync` / `GetXxxTreeOptionsAsync` | **本章扩展**（非替代 CRUD） |
| 前端 | QueryBar/ToolsBar/takt-modal/**权限** 遵守 `12-crud`；树区用 `takt-tree-*` |

---

## 二、后端实体（强制字段）

| 字段 | 说明 |
|------|------|
| `ParentId` | `long`，**0 表示根** |
| `SortOrder` | 同级排序；新建时可用 `ITaktSortOrderGenerator` |
| `Level` | 层级（1=根下一级），Create/Update 时维护 |
| `DeptPath` / `XxxPath` | 物化路径（如 `/1/3/5/`），便于查子孙（可选但推荐） |
| `IsLeaf` | 是否叶子（可选，维护于增删子节点后） |

索引：`(TenantCode, CompanyCode, ParentId)`、`DeptCode` 等业务唯一键。

---

## 三、后端 DTO 与服务

### 3.1 DTO

```csharp
public class TaktXxxTreeDto : TaktXxxDto
{
    public List<TaktXxxTreeDto> Children { get; set; } = new();
}
```

- 树接口返回 `List<TaktXxxTreeDto>`，**非** `TaktPagedResult`
- 下拉选项：`List<TaktTreeSelectOption>`（`GetXxxTreeOptionsAsync`）

### 3.2 服务方法（命名固定）

| 方法 | 返回值 | 说明 |
|------|--------|------|
| `GetXxxTreeAsync(long parentId = 0, bool includeDisabled = false)` | `List<TaktXxxTreeDto>` | 管理页树数据 |
| `GetXxxTreeOptionsAsync()` | `List<TaktTreeSelectOption>` | 表单父级选择 |
| `GetXxxListAsync` | `TaktPagedResult<TaktXxxDto>` | 可选：平铺分页（非树 UI 时） |

### 3.3 BuildTree 私有方法

**标准参照**：`TaktDeptService.BuildDeptTree` / `BuildDeptTreeOptions`

```csharp
private List<TaktXxxTreeDto> BuildXxxTree(List<TaktXxx> allRecords, long parentId)
{
    return allRecords
        .Where(x => x.ParentId == parentId)
        .OrderBy(x => x.SortOrder)
        .Select(item => { /* Adapt + 递归 Children */ })
        .ToList();
}
```

- 一次查租户+公司下全表（或合理过滤），**内存构树**；数据量极大时评估 `DeptPath` 前缀查询
- 禁止 N+1 递归查库

### 3.4 Create / Update / Delete

- **Create**：校验 `ParentId` 存在；禁止挂到自身子级（Update 改父时做环检测）
- **Update**：改 `ParentId` / `SortOrder` 时同步 `Level`、`Path`、`IsLeaf`
- **Delete**：有子节点禁止删（或级联策略写清）；内置节点 `IsBuiltIn` 禁止删

### 3.5 控制器路由

```
GET api/TaktDepts/tree?parentId=0&includeDisabled=false
GET api/TaktDepts/tree-options
```

方法名与路由段与 `TaktDeptService` 对齐；权限独立：`humanresource:organization:dept:list` 等。

---

## 四、前端实现

**标准参照**：`views/human-resource/organization/dept/index.vue`

### 4.1 布局模式

视图布局与分区见 `13-vue-view` §四；树 API/拍平见下文。

| 模式 | 组件 | 说明 |
|------|------|------|
| 左树右表 | `takt-tree-left-table` + `takt-tree-right-table` + `takt-tree-left-tools-bar` | 1/4 + 3/4；左侧选节点过滤右侧 |
| 单树表 | `TaktSingleTable` / `a-table` + `children` 列 | 数据量小 |
| 表单父级 | `TaktTreeSelect` / API `getXxxTreeOptions` | 选 ParentId（`14-vue-form` §四） |

### 4.2 树数据 → 表格（强制）

```
API getXxxTree → 内存保留 fullTableTree
右侧展示：选中左侧节点 → getSubtree → flattenXxxRows（深度优先拍平）→ 客户端分页 slice
```

- **禁止**把整棵树未拍平直接绑大表格不分页
- `takt-tree-right-table`：`showPagination` + `virtual`（拍平行数超阈值自动 virtual，见 `07-overflow-vue`）
- `expandedRowKeys` / `children`：树表展开列与拍平策略二选一，勿重复渲染

### 4.3 客户端工具函数（页面内）

- `flattenDeptTableRows`：DFS 拍平，可带 `_treeDepth` 缩进
- `getSubtree(tree, key)`：按选中节点截取子树
- `collectTreeExpandableKeys`：左侧树展开全部
- `findParentAndOrderNum`：拖拽排序后算 `parentId` + `sortOrder`

### 4.4 拖拽改父级

- 左侧树 `@drop` → `updateXxx(id, { parentId, sortOrder, …其余字段 })` → 重新 `loadData`
- Id 全程 **string**

### 4.5 API / types

```typescript
export function getDeptTree(parentId: string, includeDisabled: boolean): Promise<DeptTree[]>
```

- `DeptTree` 含 `children?: DeptTree[]`；主键 `deptId: string`

---

## 五、溢出与边界

| 项 | 限制 |
|----|------|
| 树深度 | 业务建议 ≤ **10** 层（与流程设计器一致） |
| 单次构树 | 租户+公司 scoped；超大规模改用 Path 前缀查询 + 懒加载子节点 |
| 右侧拍平 | 客户端分页；行数极大时必须 virtual |
| 左侧搜索 | 客户端过滤树，不重复打全量 API |

---

## 六、Agent 约束

- 新建树模块：**先通过 `12-crud` 自检（节点 CRUD）**，再读 `TaktDeptService` + `dept/index.vue`
- 必须有 `GetXxxTreeAsync` + `BuildXxxTree`；表单选父加 `GetXxxTreeOptionsAsync`
- 前端树表页必须 **拍平 + 分页** 或 **virtual**，禁止万级 DOM
- `ParentId` 前后端类型一致（API string / DB long + JsonConverter）
- 改父级/排序须防环、防挂到自身子孙
- 视图/表单：`13-vue-view`、`14-vue-form`；后端可生成、Vue 须手工（`15-codegen` §四）
- 实现清单：`.cursor/skills/11-tree-table/SKILL.md`
