---
description: Takt 树表（ParentId 层级）前后端规范；与 00-project、01-backend、02-frontend、07-overflow 一并生效
globs: backend/**/TaktDept*.cs,backend/**/TaktMenu*.cs,backend/**/*Tree*.cs,frontend/src/components/business/takt-tree-*/**/*,frontend/src/views/human-resource/organization/dept/**/*
alwaysApply: true
---

# 树表（Tree Table）规范

**强制基线**：树节点实体须遵守 `@.cursor/rules/12-crud.mdc`。本章规定 **ParentId 层级与树形 UI 扩展**。

**代码生成**：实体含 `ParentId` 时，`generate-all` 生成懒加载 `Get*TreeAsync(parentId)` / `TreeOptions(parentId)`（一层子节点）+ Vue 懒加载左树（`15-codegen`）；排除实体（Dept/Menu 等）Vue 须手工对齐 `admin-division`。

通用命名、权限、i18n 见 `@.cursor/rules/00-project.mdc`；后端见 `01-backend`；前端见 `02-frontend`；树深/虚拟列表见 `07-overflow-vue` §3。

**代码生成模板类型**：`TaktGenTable.GenTemplateCategory = tree`（对应 `dict.code.generator.template.type.tree`）。

---

## 一、适用场景

| 场景 | 示例 |
|------|------|
| 组织/分类层级 | 部门、菜单、成本中心、科目 |
| 左树右表 | 左侧导航树 + 右侧树形表格（部门管理） |
| 树选择 | `TaktTreeSelectOption` 下拉/表单选父节点 |

❌ 单据头+明细（外键一对多）→ 见 `@.cursor/rules/10-master-detail.mdc`

---

## 一·五、与 CRUD 基线的关系

树模块 **不是** 只有 `tree` 接口——节点实体仍须完整 CRUD，参照 `TaktDeptService`：

| 能力 | 树模块中的落点 |
|------|----------------|
| `GetXxxListAsync` | 平铺分页列表（可选，与树并存） |
| `GetXxxByIdAsync` / `CreateXxxAsync` / `UpdateXxxAsync` / `DeleteXxxByIdAsync` | 表单增删改、拖拽改 `ParentId` |
| `GetXxxTreeAsync` / `GetXxxTreeOptionsAsync` | **本章扩展**（非替代 CRUD） |
| 前端 | QueryBar/ToolsBar/takt-modal/**权限** 遵守 `12-crud`；树区用 `takt-tree-*` |

---

## 二、后端实体（强制字段）

| 字段 | 说明 |
|------|------|
| `ParentId` | `long`，**0 表示根** |
| `SortOrder` | 同级排序；新建时可用 `ITaktSortOrderGenerator` |
| `Level` | 层级（1=根下一级），Create/Update 时维护 |
| `DeptPath` / `XxxPath` | 物化路径（如 `/1/3/5/`），便于查子孙（可选但推荐） |
| `IsLeaf` | 是否叶子（可选，维护于增删子节点后） |

索引：`(TenantCode, CompanyCode, ParentId)`、`DeptCode` 等业务唯一键。

---

## 三、后端 DTO 与服务（大数据：懒加载一层）

### 3.1 DTO

```csharp
public class TaktXxxTreeDto : TaktXxxDto
{
    public List<TaktXxxTreeDto> Children { get; set; } = new();
}
```

- 树接口返回 `List<TaktXxxTreeDto>`（**仅一层直接子节点**），**非** `TaktPagedResult`
- 下拉：`List<TaktTreeSelectOption>`，`IsLeaf` + `Children = null`（见 `TaktLazyTreeHelper`）

### 3.2 服务方法（命名固定）

| 方法 | 返回值 | 说明 |
|------|--------|------|
| `GetXxxTreeAsync(long parentId = 0, bool includeDisabled = false)` | `List<TaktXxxTreeDto>` | **仅** `ParentId == parentId` 一层 |
| `GetXxxTreeOptionsAsync(long parentId = 0)` | `List<TaktTreeSelectOption>` | 表单父级懒加载，带 `parentId` |
| `GetXxxListAsync` | `TaktPagedResult<TaktXxxDto>` | 右侧表服务端分页（Query 含 `ParentId`） |

### 3.3 禁止全表递归 Build（强制）

**标准参照**：`TaktAdminDivisionService` + `TaktLazyTreeHelper`  
**生成脚本**：`generate-services-from-dtos.cjs` → `generateTreeServiceMethods`

```csharp
// ✅ 只查直接子级
var list = await repo.GetListAsync(x => … && x.ParentId == parentId …);
return list.OrderBy(…).Select(item => {
    var dto = item.Adapt<TaktXxxTreeDto>();
    dto.Children = new List<TaktXxxTreeDto>(); // 非递归；靠 IsLeaf / 前端 loadData
    return dto;
}).ToList();
```

```text
❌ GetListAsync 全表 + BuildXxxTree 内存递归
❌ TreeOptions 无 parentId、一次返回整棵树
```

### 3.4 Create / Update / Delete

- **Create**：校验 `ParentId` 存在；禁止挂到自身子级（Update 改父时做环检测）
- **Update**：改 `ParentId` / `SortOrder` 时同步 `Level`、`Path`、`IsLeaf`
- **Delete**：有子节点禁止删（或级联策略写清）；内置节点 `IsBuiltIn` 禁止删

### 3.5 控制器路由

```
GET api/TaktXxxs/tree?parentId=0&includeDisabled=false
GET api/TaktXxxs/tree-options?parentId=0
```

权限与 `12-crud` / 菜单种子一致。

---

## 四、前端实现

**标准参照（大数据）**：`views/foundation/admin-division/index.vue`  
**代码生成**：`scripts/gen/generate-vue-tree-from-api.cjs`（流水线步 9 / `generate-vue-all`）按本节生成，**禁止**再生成「一次拉全量树 + 客户端拍平」。

### 4.1 布局模式

视图布局与分区见 `13-vue-view` §四。

| 模式 | 组件 | 说明 |
|------|------|------|
| 左树右表 | `takt-tree-left-table` + `takt-tree-right-table` | 左侧懒加载导航；右侧 `getXxxList` 服务端分页 |
| 表单父级 | `TaktTreeSelect` + `:lazy="true"` + `tree-options?parentId=` | 选 ParentId（`14-vue-form` §四） |

### 4.2 大数据树数据流（强制）

```
左侧：getXxxTree(parentId) 仅一层 → mapLazyTreeNodes → :load-data 展开再拉
     + :virtual="true"（禁止一次渲染万级 DOM）
右侧：getXxxList({ parentId, pageIndex, pageSize, … }) 服务端分页
     选中左侧节点 → parentId 换源 → 重新 list
```

- **禁止** `getXxxTree('0')` 拉整棵树进 `fullTableTree` 再 `flatten` + 客户端 slice
- **禁止** 左侧 `:virtual="false"` 绑全量树节点
- 展开「全部」仅作用于**已加载**节点（`collectTreeExpandableKeys`），不触发全量展开
- 表单 `TaktTreeSelect`：**必须** `:lazy="true"`（与 `use-lazy-tree` / `takt-tree-select` 对齐）

### 4.3 页面内编排（生成脚本产出）

- `mapXxxLazyNodes` / `mergeLoadedChildren` / `taktIsLeafFlag`（`@/composables/use-lazy-tree`）
- `reloadLeftTreeRoots` / `handleLeftTreeLoadData` / `loadRightList` / `refreshAfterMutation`
- `findParentAndOrderNum`：拖拽改 `parentId` + `sortOrder`

### 4.4 拖拽改父级

- 左侧树 `@tree-drop` → `updateXxx` → `loadData` / `refreshAfterMutation`
- Id 全程 **string**

### 4.5 API / types

```typescript
export function getXxxTree(parentId: string, includeDisabled: boolean): Promise<XxxTree[]>
export function getXxxList(query: { parentId?: string; pageIndex: number; pageSize: number }): Promise<TaktPagedResult<Xxx>>
```

- 树 DTO 一层即可；`isLeaf` 驱动展开图标；主键 **string**

---

## 五、溢出与边界

| 项 | 限制 |
|----|------|
| 树深度 | 业务建议 ≤ **10** 层 |
| 左侧渲染 | **必须** virtual + 懒加载；禁止万级 DOM |
| 右侧列表 | **必须** 服务端分页（`TaktPagedQuery`）；禁止全量拍平 |
| 左侧搜索 | 仅过滤已加载节点 |

---

## 六、Agent 约束

- 新建/再生树页：**改前台流水脚本** `generate-vue-tree-from-api.cjs`，对照 `admin-division/index.vue`
- 前端树表页必须 **懒加载 + virtual + 右侧 list 分页**，禁止全量树拍平
- `ParentId` 前后端类型一致（API string / DB long）
- 视图/表单：`13-vue-view`、`14-vue-form`；生成后人工核对排除实体（Dept/Menu 等）
- 实现清单：`.cursor/skills/11-tree-table/SKILL.md`
